const Loading = () => {
  return (
    <div
      className="w-[340px] h-[340px] flex justify-center items-center relative overflow-hidden animate-backgroundShift rounded-4xl"
      style={{ background: `var(--bg-main)` }}
    ></div>
  );
};

export default Loading;
